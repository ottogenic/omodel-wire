# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local overrides / secrets — do not commit
*.local.json
default_models.json
.env
hf_token
wire.json
hosts

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Agent-harness / AI-tool scaffolding — keep the repo tool-agnostic.
# These are per-developer, auto-generated by whatever AI tooling you happen to
# run locally; they are not part of omodel-wire and should never be committed.
CLAUDE.md
CLAUDE.local.md
.claude/*
!.claude/skills/
.cursor/
.cursorrules
.windsurfrules
.aider*
.github/copilot-instructions.md
GEMINI.md
.opencode/

# omw proxy runtime artifacts (never commit)
proxy_logs/
proxy_routes.json
*.proxy-bak
.omw-proxy.pid
